import { createElement, Fragment } from "react";
/**
 * WordPress dependencies
 */
import { BlockSettingsMenuControls } from '@wordpress/block-editor';

/**
 * Internal dependencies
 */
import ReusableBlockConvertButton from './reusable-block-convert-button';
import ReusableBlocksManageButton from './reusable-blocks-manage-button';
export default function ReusableBlocksMenuItems({
  rootClientId
}) {
  return createElement(BlockSettingsMenuControls, null, ({
    onClose,
    selectedClientIds
  }) => createElement(Fragment, null, createElement(ReusableBlockConvertButton, {
    clientIds: selectedClientIds,
    rootClientId: rootClientId,
    onClose: onClose
  }), selectedClientIds.length === 1 && createElement(ReusableBlocksManageButton, {
    clientId: selectedClientIds[0]
  })));
}
//# sourceMappingURL=index.js.map